# Цитадель v72 — исправление белого экрана

Исправлено:
- сайт больше не зависит от отдельной загрузки app.js/style.css на GitHub Pages;
- весь рабочий интерфейс встроен прямо в index.html;
- устранена проблема рассинхронизации кэша GitHub Pages между index.html и app.js;
- чтение localStorage защищено от ошибок;
- bootstrap защищён от падения;
- если браузерный кэш повреждён, вместо белого экрана появляется кнопка восстановления.

Сохранена вся функциональность v71 и текущая ссылка Google Apps Script.

Что загружать:
можно просто заменить ВСЕ файлы из архива.
Главный файл — index.html.

Apps Script повторно менять не требуется, если текущий backend уже опубликован.
После загрузки сделать Ctrl+F5.
